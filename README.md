The config.json file should be in this format:

{
	"token": "tokenhere"
}


and for the attendance script there should be a google cloud auth file in your documents with the name junkyard-google-cloud-credentials.json